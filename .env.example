# ===== LLM API Configuration =====
# LLM API 配置

# API Key (必填)
api-key=your-api-key-here

# API Base URL (必填)
base-url=https://api.siliconflow.cn/v1

# Model Name (可选，默认值如下)
model=Qwen/Qwen2.5-7B-Instruct

# Temperature (可选，默认 0.3)
temperature=0.3

# Max Iterations (可选，默认 10)
max_iterations=10

# Data Directory (可选，默认 data)
data_dir=data
